# TablePlus: always open as a floating window
# Run `hyprctl clients` with TablePlus open to verify the class name if this doesn't work
windowrule = float on, match:class TablePlus
windowrule = center on, match:class TablePlus
windowrule = size 1400 900, match:class TablePlus
